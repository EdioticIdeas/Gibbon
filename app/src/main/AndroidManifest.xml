<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ideotic.edioticideas.gibbon">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ape_monkey_only"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TimetableActivity"
            android:label="@string/title_activity_timetable"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TeacherDashboardActivity"
            android:label="@string/title_activity_teacher_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Timetable_TeacherActivity"
            android:label="Time Table"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChatAct" />
        <activity android:name=".ViewAttendanceActivity" />
        <activity android:name=".ViewStudentDetailsActivity"></activity>
    </application>

</manifest>